export type ProductItem = {
  productName: string;
  productPrice: number;
  productDescription: string;
  productCalories: number;
  imageUrl: string;
};

export type ProductResponse = {
  id: number;
  productName: string;
  productPrice: number;
  productDescription: string;
  productCalories: number;
  imageUrl: string;
};
